extends layout

block content
  .w3-container.w3-margin
    h1 Classes de nível 1

    table.w3-table-all
      tr
        th Código
        th Título
      each c in lista
        tr
          td
            a(href='/classes/c' + c.codigo)= c.codigo
          td= c.titulo

    br

    address [ 
      a(href='/') Página inicial 
      | ]